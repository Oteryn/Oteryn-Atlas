# Atlas context-routing canary — META #166

Status: candidate preparation; not adopted policy, not a completed programme.

Scope comes from the owner-invoked `Oteryn: context optimization` programme in Oteryn/Oteryn#166. Atlas #315 continues to govern maintenance. This evidence is task-only, not a second lifecycle registry or agent bootstrap.

## Admission

- Protected main: `1443615e10cec21d481380b289dcbb742415d709`; tree `9bee955f9222139345d124a963a72c46118e790e`.
- Root `AGENTS.md`: blob `92f8b48bd68ce415dc3911519e562e54e6c68804`, 7855 bytes, 63 lines; isolated source copy authenticated by Git blob SHA.
- Binding: `docs/agents/META_AGENT_POLICY_BINDING.json`, blob `cef7245d3bb9120126380b027de8efb3785949e7`, immutable META `1dedfc0f264fe0e23e5365dbe9280c2d96df50c5`, policy 3.0.0. Bound organization, prompting and evaluation files match the authenticated sources read by the coordinator.
- Root/docs/docs-agents Git trees contain no nearer applicable AGENTS override. New specialist files remain under the maintenance-admitted `docs/agents/**` path family.
- `docs/agents/DOCUMENTATION_AGENT_IA.md` remains unchanged: live Issues govern lifecycle; historical prompt presence is not dispatch authority; no duplicate registry.
- #315 comment `5559782784` records the completed normal protected #346 integration. The older issue-body statement that maintenance is not active is historical.
- PR #344's complete changed-file list has no overlap with the canary paths; its UI/runtime work is not part of this task.

## Candidate allocation

One coordinator/writer, branch `docs/issue-166-context-routing`. Owned paths: `AGENTS.md`, `docs/agents/operations/VERIFICATION_CAPABILITY.md`, `docs/agents/operations/LIVE_DEPLOYMENT.md`, and this evidence file.

The candidate will retain the whole maintenance block, local authority/security bootstrap, all projection/provenance/rendering invariants, and normal exact-head integration requirements. It will relocate the seven verification-capability bullets and three live-deployment bullets verbatim behind explicit pre-operation read triggers. No workflow, validator, test source, policy binding, protection, product, publication or deployment change is allocated.

## Acceptance boundary

The initial commit is a real admission/evidence record so an early Draft PR can supply its actual identity to the canonical routing preflight; it changes no active instructions. Exact source preservation, full diff review and protected-base maintenance CI will qualify only the deterministic candidate. They do not establish actual client delivery or model behavior.

Matched before/after Sol and Astra trials and independent review remain NOT_EVALUATED: the current session exposes no invokable isolated-worker/model-effort evaluator. No model trial or token/cost measurement has been performed. Keep this canary Draft, with no enqueue, auto-merge or direct merge until the required behavioral evidence exists. META #166 and Atlas #315 remain open; maintenance continues unchanged.

The consolidated disposition, measurement method, eight-family trial cases and missing capability are recorded in META PR #168 under `docs/evidence/context-optimization-20260907/`. Source byte changes are not delivered-token or cost savings.
