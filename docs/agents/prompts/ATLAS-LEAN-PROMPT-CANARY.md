# ATLAS-LEAN-PROMPT-CANARY

Lifecycle: Oteryn/Oteryn-Atlas#307
Parent: Oteryn/Oteryn#140

## Outcome

Prove that an Atlas reusable GPT-5.6 Sol prompt can remain a task-specific delta while repository and programme authorities continue to govern execution, review, verification and integration.

## Scope

- Reconcile Atlas prompt lifecycle metadata on current protected `main`.
- Keep this canary documentation/governance-only.
- Do not modify runtime, publication data, verification architecture, deployment, secrets, branch protection or other repositories.
- Do not modify root `AGENTS.md` while another active PR owns it.

## Atlas invariants

- Oteryn-Game remains canonical World/Content authority; Atlas is a derived semantic projection.
- Preserve Atlas provenance, browser/render, FullWorld, specialist-runner and merged-main deployment boundaries.
- Inherit repository-wide execution, authorization, review and Merge Queue policy from current Atlas authority instead of copying it into this prompt.
- GitHub Issue #307 owns mutable lifecycle state.

## Acceptance

- `DOCUMENTATION_AGENT_IA.json` matches the current prompt and active-task filesystem and current Issue lifecycle states.
- Completed prompt/task lifecycles are historical or archived rather than apparently dispatchable.
- The deterministic lean-prompt regression accepts this task-delta structure and rejects copied repository-policy sections.
- Exact-head Atlas checks and independent review are clean before normal protected integration.
