# ATLAS-R4-SHADOW-ACTIVATION-LEAD

Lifecycle: `Oteryn/Oteryn-Atlas#315`

Alias: `Oteryn: Atlas R4 Shadow Lead`

Owner invocation of this alias authorizes **R4 only**. The file's existence is not standing authority. This prompt is a task-specific delta over current `AGENTS.md` and the bound META policy.

## Outcome

Implement and qualify the **smallest real non-blocking product-verification shadow** on top of the completed R1–R3 architecture.

Target path:

`changed files -> protected planning -> semantic groups -> minimum data capability -> exact commands -> real execution -> authenticated evidence -> non-blocking shadow result`

R4 proves the architecture through real GitHub execution; it does not build the future verification platform.

At start, refresh live GitHub once. Expected checkpoint is `main@1e68e3ca2d1bf94c18ed37d79fcf7f0424e31fc2`, Issue #315 `R4_READY / activation paused`, #392 bootstrap consumed, #393 integrated normally. If unchanged, do not repeat historical audits.

## Scope

Programme lead: **Astra Mid**.

Routing:
- Astra Low: inventories, exact comparisons, logs, timing/runner metrics, bounded mechanical work.
- Astra Mid: implementation, workflow wiring, integration, PR/Merge Queue handling.
- Sol Medium: bounded semantic ambiguity when useful.
- Sol High: trust boundary, execution authority, evidence acceptance, activation design, final adversarial review. `CHANGES_REQUIRED` blocks integration.

High effort is not the default. Large scope alone is not a reason to escalate.

Token/context rule: **READ ONCE PER REVISION**. Keep a compact ledger of main SHA, #315 authority, active workflows/ruleset, relevant file/blob SHA, extracted invariant and invalidation condition. Do not reread unchanged large files or pass the full parent transcript to subagents. Give each lane only shared facts, exact files, unresolved question and expected output. Independent Sol may freshly read the small safety-critical source set.

Authorized R4 work:
- one minimal non-blocking shadow workflow or equivalent single active surface;
- protected-base planning/execution resolution;
- read-only candidate execution;
- real deterministic execution;
- real `qualification_fixture` browser execution;
- real `bounded_real_world` source-contract execution;
- minimum evidence production/validation needed by those executions;
- PR/MQ semantic-equivalence proof;
- wall-time/runner-use measurements;
- focused tests, independent Sol review, normal protected integration.

Not authorized:
- R5/product canaries;
- blocking promotion or required-check/ruleset changes;
- `real_fullworld`, Molehill or specialist FullWorld qualification;
- publication or deployment;
- secrets/environment changes;
- production cutover.

Initial shadow matrix, in order:
- **S0 docs/no-work:** protected planner selects zero product commands and no product runner starts; truthful `NO_PRODUCT_WORK`.
- **S1 deterministic:** exact candidate identity, exact command/test census, actual candidate execution, failure propagation, zero retries.
- **S2 qualification browser:** smallest truthful existing browser group; GitHub-hosted, immutable qualification fixture, exact census, `workers=1`, `retries=0`, no FullWorld/Molehill.
- **S3 bounded real:** established minimum source-contract route; authenticate required repository/revision/source bytes; no complete FullWorld build.

Use at most 3–4 genuinely path-disjoint lanes. One writer owns the active workflow and central planner/execution/evidence integration.

## Atlas invariants

Protected policy remains authority. Candidate bytes are test subjects, never policy authority.

Protected side owns planning, catalog/policy, command resolution and evidence interpretation. Candidate execution must not receive repository write credentials and must not use elevated `pull_request_target` credentials for untrusted code.

Candidate metadata may widen proof but may not narrow protected obligations, control interpreter/argv, define protected ownership, authenticate its own PASS, or change integration authority.

Verification profile and data capability stay separate:
- `qualification_fixture` is the default for ordinary behavior;
- `bounded_real_world` only where selected real bytes are part of the oracle;
- `real_fullworld` is outside initial R4. If selected, report unresolved/not executed rather than substituting fixture evidence.

Equivalent PR and Merge Queue content must select equivalent semantic groups, capabilities, expected tests and resource classes. Run identity may differ. At least one real Merge Queue shadow execution is required before R4 completion.

Transport must be concrete and minimal. Before adding a module ask whether the active R4 workflow consumes it now and whether an existing layer suffices. Do not introduce a generic producer framework, receipt bus, event-dispatch framework, promotion engine, multi-workflow orchestrator, generic ZIP layer or controller state machine unless the single R4 workflow demonstrably requires it.

Record compact machine evidence only where useful: repository, candidate/base identity, plan digest, groups, capability/resource, command/test census, exit status, actual test census where available, duration, retries/skips and artifact digest where an artifact is necessary. GitHub run IDs plus a compact durable summary are preferred to copied logs. A `PASS` string alone is not authenticated evidence.

Fail closed on unknown impact, identity drift, missing expected tests, unexpected commands/runtime identities, source mismatch, missing bounded-real proof or stale candidate after planning. Do not repair with retries, sleeps, unjustified widening, FullWorld escalation, fabricated receipts or skips.

If control-plane admission blocks the minimal R4 change, do not invent an ad-hoc bootstrap. Sol High must first prove why current future-stable authority is insufficient and define the smallest structural transition. Never infer bypass authorization.

Use RED -> GREEN focused tests. Run the full deterministic/maintenance suite only when shared semantics change. Run real browser shadow only after local semantic tests are green.

## Acceptance

R4 is complete only when all are true:
- one minimal non-blocking shadow surface is integrated;
- S0, S1, S2 and S3 executed truthfully;
- at least one real Merge Queue shadow execution is verified;
- protected/candidate authority separation holds;
- exact execution/evidence census is coherent;
- retries remain zero and skips are explained/absent for required proofs;
- ordinary E2E has no FullWorld/Molehill dependency;
- required checks/rulesets remain unchanged;
- publication/deployment remain inactive;
- wall time, runner minutes/jobs and avoided unnecessary work are measured where observable;
- independent Sol High final verdict is `PASS`.

Final Sol review must explicitly check: protected planning authority, candidate non-authority, docs/no-work truthfulness, real deterministic/browser/bounded-real execution, PR/MQ equivalence, zero retries, FullWorld exclusion, non-blocking behavior, unchanged protection, and absence of premature R5/deployment machinery.

On success update #315 to `R4_COMPLETE / R5_NOT_AUTHORIZED` and stop. Do not start R5 automatically.

Terminal report: protected main SHA, workflow path, PR/MQ run IDs, S0–S3 results, groups/capabilities, retries/skips, runner/wall-time measurements, Sol verdict, remaining UNKNOWNs, and explicit `R5_NOT_AUTHORIZED`.

If all authorized work is exhausted behind a genuine external blocker, report exact blocker plus exact candidate/head/tree and minimal owner action; otherwise continue through temporary PR/check/MQ pending states.

Core rule: **activate the smallest truthful shadow that proves R1–R3 with real execution; do not rebuild the retired control plane under new names.**
