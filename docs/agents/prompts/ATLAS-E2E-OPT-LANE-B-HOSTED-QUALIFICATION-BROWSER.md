# ATLAS-E2E-OPT-LANE-B-HOSTED-QUALIFICATION-BROWSER

ALIAS:
`ATLAS-E2E-OPT-LANE-B-HOSTED-QUALIFICATION-BROWSER`

EFFORT: Extra High

SCOPE:
Finish the self-contained GitHub-hosted `qualification_fixture` browser path and ordinary full-safety proof for final Phase D. Work in an isolated branch/worktree. Do NOT alter protected controller authority, merge protected PRs, or enable selective execution.

MANDATORY START:
Resolve #213 fresh and inspect the latest hosted-full-safe commits before writing code. Also inspect branch `handoff/issue-179-ancillary-hosted-q`; cherry-pick only if its delta remains useful against current #213.

KNOWN HANDOFF DELTA:
The handoff branch is based on a then-current #213 and contains neutral fixture animation/creature/search ancillary products plus hosted namespace mounts. Targeted verification on that branch passed 16/16 for hosted publication/full-safe/ancillary contracts at handoff creation. This is evidence to revalidate, not authority to trust blindly.

GOAL:
Ordinary NPC/monster/cards/inspector/search/state/history/geometry/pan/zoom/LOD/a11y/responsive/race/fault/gameplay-shell E2E must run against a minimal immutable fixture through production seams with no Synology/LAN/real-FullWorld dependency.

REQUIRED PROPERTIES:
- fixture is immutable/content-addressed and verified before readiness;
- production publication/semantic/floor/chunk/HTTP-Range/digest/runtime-index/pixels/pixel-buckets/overview/minimap/animation/creature/search loaders are exercised;
- qualification trust is explicit, marker/fixture-ID/root/product-digest bound and malformed injection fails closed;
- production defaults stay pinned to production identities when no qualification trust is injected;
- fixture creature/search records are neutral Atlas-owned facts, never Sam/Rat/Thais or fabricated Game authority;
- real Game gameplay facts remain isolated in `bounded_real_world` tests; Q gameplay verifies shell/state/touch/Escape/unavailable behavior only;
- hosted web must not leak checkout real `/web/semantic-search/**`, `/web/creature-gameplay/**` or LAN `/data/creatures/**` into Q;
- static fixture server preserves real Range 206/416 behavior and revalidates after readiness; tamper/truncate/missing/duplicate/early-read cases fail closed;
- no sleeps as readiness.

TEST/DELIVERABLE:
1. Re-run targeted qualification builder/loader/publication/compose/full-safe tests.
2. Run exact ordinary Playwright stable-ID set selected by current hosted-full-safe resolver, zero retries.
3. Record summary identity, exact stable IDs, fixture product digest and absence of real-FullWorld groups.
4. Visual evidence must follow the repository review contract; do not auto-approve screenshots.
5. Commit/push a dedicated lane branch/PR with exact base/head/test results and no #179 completion claim.

DO NOT:
- run full ~19 GB FullWorld because an ordinary oracle does not need it;
- weaken assertions/tolerances;
- copy real Game data into qualification fixture just to make a test pass;
- change the protected planner/fan-in except for a narrowly necessary interface contract coordinated with Lane A.
