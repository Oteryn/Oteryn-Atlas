# R4 supervision checkpoint

Status: active R4 supervision checkpoint for Issue #315.

Protected baseline at capture: `main@ae31c3032534241e61f4108d8e25144bc7687bd0`.
This document is evidence/context only; it does not grant integration authority or authorize R5.

## Integrated R4 progress

- #396 integrated the reviewed structural R4 preparation under one exact owner-authorized transition. That exception is consumed and is not standing authority.
- #397 activated one nonblocking `.github/workflows/verification-shadow.yml` through ordinary protected integration.
- #398 proved S0 docs/no-work in both PR and actual Merge Queue shadow: zero product commands and execute skipped.
- #400 corrected real hosted fixture-proof and candidate-execution-view failures through ordinary protected integration.
- #402 added bounded failed-command diagnostics only; it did not weaken evidence or retry policy.
- S3 `bounded_real_world` has actually executed the selected Game-producer-derived source contract: two exact Playwright HTTP tests passed with zero retries/skips and authenticated pinned source/output evidence. No FullWorld claim is made.

## Current qualification candidate

PR #399 is the live S1/S3 qualification candidate. At capture its current candidate contains only:

- `tests/verification/verification-execution-contract.test.mjs`;
- `e2e/tests/creature-gameplay-source-contract-desktop.spec.mjs`.

The intentional negative path-traversal assertion proves candidate failure propagation: protected execution still rejects the unsafe path.

A hosted run demonstrated that deterministic execution now starts correctly, but the current plan selects the complete `deterministic.core` command set. The run therefore starts roughly 149 deterministic containers for a tiny qualification candidate.

This is not an acceptable steady-state proof shape for R4 unless a protected oracle independently proves that the complete deterministic corpus is required.

## Supervisory finding S1

R4 requested the smallest representative deterministic proof.

Preferred closure:

- qualify S1 with one newly added safe deterministic candidate test;
- use the existing post-#393 unprivileged candidate-test semantics;
- candidate test executes self-only under protected interpreter/argv;
- intentional failure is visible;
- candidate gains no ownership, dependency-edge or policy authority;
- target proof shape is approximately `1 changed candidate test -> 1 deterministic command/container`.

Do not widen the isolated runtime merely to make unrelated historical `deterministic.core` tests pass.

The observed `protected-admission-cli-census.test.mjs` failure is `spawnSync gh EACCES`. Before changing runtime capabilities, first determine whether that test should be in S1 at all. If S1 is narrowed correctly, this historical CLI test should normally be outside the representative S1 proof.

## Supervisory finding S2

Draft #401 prepares an exact maintenance admission for modifying `e2e/tests/layer-audit-desktop.spec.mjs` so S2 can be qualified.

Do not treat #401 as automatically authorized.

Before any owner decision, require fresh independent Sol High review of the exact current head answering:

1. Is `layer-audit-desktop.spec.mjs` still the smallest truthful S2 qualification-fixture owner?
2. Is there an already admitted qualification-fixture browser route that can prove S2 without a new authority transition?
3. Why was this S2 admission need not included in the structural R4 preparation #396?
4. If #401 is still necessary, does it create only the final exact capability needed for S2, with no recurring equivalent bootstrap class?

Only after that review may an exact owner decision be requested. No standing bypass exists.

## Scope invariants

R4 remains nonblocking.

Still excluded:

- R5;
- required-check or ruleset promotion;
- `real_fullworld`;
- Molehill/PC FullWorld execution;
- publication;
- deployment;
- secrets/environment mutation.

Verification profile remains separate from data capability. Ordinary functional browser proof uses `qualification_fixture`; selected real-source proof uses `bounded_real_world` only when source bytes are part of the oracle.

## Supervision priority

Optimize correctness first, then minimize runner and context cost.

Do not let R4 become a generic CI platform. Prefer one protected planner, one concrete nonblocking workflow, existing execution contracts, compact evidence, zero retries and minimum truthful command/test census.

Before declaring R4 complete, require real S0/S1/S2/S3 evidence, actual Merge Queue shadow evidence, measurements, and independent Sol High PASS. Final state remains `R4_COMPLETE / R5_NOT_AUTHORIZED`.