# Inactive R1–R3 execution contract

Protected baseline: `6a4172d518dd85b6447d6a4785eb0f87fc6c8a46`.
Lifecycle authority: [Issue #315](https://github.com/Oteryn/Oteryn-Atlas/issues/315), refreshed 2026-09-08. R1–R3 implementation and an exact admission-transition proposal are authorized. R4, product workflows, shadow execution, canaries, promotion, publication and deployment remain paused. The #387 one-time bypass is consumed.

## Current execution boundary

`buildVerificationPlan` unions protected and candidate impact obligations. Unknown paths, unowned tests and unresolved complete-product obligations retain explicit blockers. `resolveExecutionContract` rebuilds that plan, compares selected groups with protected executable policy, resolves exact deterministic or Playwright commands and binds candidate head/base/tree/diff, policy, environment and plan identities. Candidate metadata can demand additional proof; it cannot supply executable authority.

The deterministic resolver checks source digests and reviewed import/subprocess edges, rejects unsafe paths and overlapping execution roots, and deduplicates covered tests. The browser resolver authenticates actual publication bytes and exact source identities before returning commands. HTTP-only Playwright has a project and test census despite `browser=false`. Machine execution and restricted frame review remain distinct obligations.

The fan-in accepts exact command/test census, zero retries, bounded current execution and downloaded report bytes. Producer receipts are process-local results of GitHub run/job/artifact association and ZIP-byte verification. Serialized candidate JSON cannot mint a receipt. Visual receipts separately require current review timeline, maintainer permission, independent actor and an exact capture bundle; machine success alone cannot discharge them. A production caller must obtain these API records from GitHub using protected code. No such workflow is activated here.

A docs-only complete plan resolves to zero commands. `e2e.full` is an aggregate fallback alias, not a runnable job. Equivalent PR/MQ changes have equivalent semantic commands while producer event/candidate identities remain separately bound.

## Canonical metadata and projections

`verification-catalog.json.executionPolicy` is the canonical ownership/execution policy. `verification-metadata.mjs` derives browser, deterministic and contract views. The three older ownership JSON files are pointer descriptors, not independent editable registries. Review stable IDs derive from frame declarations. `protected-semantic-routing.mjs` is a compatibility projection of the same planner and canonical ownership; historical property labels cannot override selection.

`protected-scenario-inventory.json` and `full-safety-net-stable-ids.json` are generated test-census snapshots, not ownership authorities. They were refreshed from actual locked Playwright `--list`: 79 cases in 36 specs, of which 74 use qualification fixtures. Future protected execution must enumerate its exact test census; these snapshots are not evidence that browser tests ran. Every one of the 30 declared review frames has a listed stable test ID.

## Semantic groups

| Group | Engine | Data capability | Resource | Evidence |
| --- | --- | --- | --- | --- |
| `deterministic.core` | deterministic | qualification_fixture | cpu-light | machine-summary |
| `e2e.common-smoke` | playwright | qualification_fixture | browser-targeted | machine-summary |
| `e2e.full` | aggregate | qualification_fixture | browser-full | machine-summary |
| `fullworld.animation-census` | playwright | real_fullworld | browser-targeted | machine-summary |
| `deterministic.search` | deterministic | qualification_fixture | cpu-light | machine-summary |
| `deterministic.gameplay` | deterministic | qualification_fixture | cpu-light | machine-summary |
| `deterministic.creature-interaction` | deterministic | qualification_fixture | cpu-light | machine-summary |
| `deterministic.creature-presentation` | deterministic | qualification_fixture | cpu-light | machine-summary |
| `deterministic.creature-products` | deterministic | qualification_fixture | cpu-light | machine-summary |
| `deterministic.farm-products` | deterministic | qualification_fixture | cpu-light | machine-summary |
| `deterministic.farm-ui` | deterministic | qualification_fixture | cpu-light | machine-summary |
| `deterministic.browser-core` | deterministic | qualification_fixture | cpu-light | machine-summary |
| `deterministic.publication` | deterministic | qualification_fixture | cpu-light | machine-summary |
| `deterministic.fullworld-runtime` | deterministic | qualification_fixture | cpu-light | machine-summary |
| `deterministic.fullworld-layers` | deterministic | qualification_fixture | cpu-light | machine-summary |
| `deterministic.fullworld-minimap` | deterministic | qualification_fixture | cpu-light | machine-summary |
| `deterministic.geometry` | deterministic | qualification_fixture | cpu-light | machine-summary |
| `deterministic.maintenance` | deterministic | qualification_fixture | cpu-light | machine-summary |
| `deterministic.deployment` | deterministic | qualification_fixture | cpu-light | machine-summary |
| `e2e.shell-accessibility` | playwright | qualification_fixture | browser-targeted | machine-summary |
| `integration.source-contract-browser` | playwright | bounded_real_world | browser-targeted | machine-summary |
| `e2e.map-navigation` | playwright | qualification_fixture | browser-targeted | machine-summary |
| `review.audit-desktop` | playwright | qualification_fixture | browser-targeted | restricted-visual-review |
| `e2e.creature-gameplay` | playwright | qualification_fixture | browser-targeted | machine-summary |
| `review.creature-gameplay-desktop` | playwright | qualification_fixture | browser-targeted | restricted-visual-review |
| `review.creature-gameplay-mobile` | playwright | qualification_fixture | browser-targeted | restricted-visual-review |
| `integration.source-contract-http` | playwright | bounded_real_world | cpu-light | machine-summary |
| `e2e.creature-interaction` | playwright | qualification_fixture | browser-targeted | machine-summary |
| `e2e.creature-presentation` | playwright | qualification_fixture | browser-targeted | machine-summary |
| `review.creature-presentation-desktop` | playwright | qualification_fixture | browser-targeted | restricted-visual-review |
| `review.creature-presentation-mobile` | playwright | qualification_fixture | browser-targeted | restricted-visual-review |
| `e2e.loader-resilience` | playwright | qualification_fixture | browser-targeted | machine-summary |
| `review.degraded-search-desktop` | playwright | qualification_fixture | browser-targeted | restricted-visual-review |
| `e2e.farm-explorer` | playwright | qualification_fixture | browser-targeted | machine-summary |
| `e2e.render-synchronization` | playwright | qualification_fixture | browser-targeted | machine-summary |
| `e2e.layer-availability` | playwright | qualification_fixture | browser-targeted | machine-summary |
| `e2e.bounded-performance` | playwright | qualification_fixture | performance | machine-summary |
| `review.resilience-desktop` | playwright | qualification_fixture | browser-targeted | restricted-visual-review |
| `e2e.bounded-soak` | playwright | qualification_fixture | soak | machine-summary |
| `e2e.search-navigation` | playwright | qualification_fixture | browser-targeted | machine-summary |
| `e2e.bounded-stress` | playwright | qualification_fixture | performance | machine-summary |
| `e2e.cross-feature-journey` | playwright | qualification_fixture | browser-targeted | machine-summary |
| `e2e.visual-presentation` | playwright | qualification_fixture | browser-targeted | machine-summary |
| `review.visual-desktop` | playwright | qualification_fixture | browser-targeted | restricted-visual-review |
| `review.visual-mobile` | playwright | qualification_fixture | browser-targeted | restricted-visual-review |
| `fullworld.complete-integrity` | complete-product | real_fullworld | artifact-build | machine-summary |

## Complete-product disposition

Ten individually reviewed producer/verifier source files plus the new independent artifact verifier select `fullworld.complete-integrity`; fixture test owners remain additive. Unknown files beneath FullWorld directories remain unresolved instead of inheriting guessed complete-world sufficiency. The CDP helper and authority registry validator retain deterministic fixture routes. Nine previously unresolved bounded transformation/consumer routes now have explicit fixture owners and additional oracle regressions.

Fullworld authentication intentionally rejects schema-only build summaries. Raw tree closure, authenticated candidate/source bytes, source-derived logical census, distinct clean build execution receipts, raw publication linkage, independent derived-product verification and bound corruption-probe receipts are required. The new local validator now independently checks runtime-index and pixel-bucket closure and recomputes minimap pixels from verified semantic/RGBA bytes. Its local receipts do not establish protected execution, source authenticity, two clean builds or real corruption-probe execution. Neither a duplicated `clean-a`/`clean-b` JSON document nor asserted `PASS` strings qualify a product. No real FullWorld build or Molehill job was executed in this slice.

Bounded-real authentication requires exactly `Oteryn/Oteryn-Game`, a protected pinned revision, exact selected source bytes, product source digests and matching publication `source.gameSha`. Fixture authentication has no real-source identity. Profiles remain independent from these byte requirements.

## Protected admission blocker

Current protected restoration rules admit many #388 files only with operation `A`. Those files now require `M`; additional historical contracts and new execution modules also lack admission. The allowlist and maintenance validator are themselves frozen. Candidate policy cannot admit its own changes. Prepare an exact A/M-only transition from the final implementation diff, retain negative boundary tests, and obtain its separate protected integration authority. Ordinary required checks and Merge Queue apply after prior admission. There is no continuing bypass authorization.

The active workflow inventory remains the three maintenance workflows. No required context or ruleset is changed by this implementation. Actual CI savings, PR/MQ product canaries and product evidence remain unmeasured because activation is paused.

## Proposed later R4 boundary — not an authorization request

Only after protected R1–R3 integration, independent final review and an explicit #315 lifecycle transition should a separate PR introduce one non-blocking product workflow with one stable fan-in. Protected-base code owns planning, command resolution, API readback and evidence validation; candidate execution receives neither integration authority nor write credentials. The first shadow increment should exercise docs-only and deterministic plans, then fixture browser and bounded-source plans. Complete-product groups remain blocked until their authenticated execution/provenance chain is implemented and qualified.

The owner transition must name the exact reviewed activation candidate and permit non-blocking shadow only. Product canaries, specialist capacity and eventual blocking promotion require their own evidence and applicable authority. Required checks must remain unchanged during shadow. No R4 authorization is being requested while implementation or protected admission remains incomplete.

## Final local qualification

The final combined verification and maintenance run passed **1076/1076**, with zero failures and skips. Exact deterministic resolution covers **203/203** tests once: 53 product entrypoints and 150 verification contracts, through 184 deduplicated root commands. The earlier 48-command product run had one failing HTTP fixture; that failure and the later bounded-oracle/rollback changes were repaired and qualified through focused runs. `validation.json` records scope and log digests.

All original 37 historical topology contracts and 20 additional stale contracts were rewritten without adding retired workflows or deleting contracts. The two deployment contracts now exercise local inactive staging/rollback and isolation semantics; actual deployment remains separately unqualified. The current protected capture projection contains all 30 canonical frames, and its generated equality is regression-tested.

Security scanning remains an explicit external UNKNOWN: the repository CodeQL workflow is archived; the prior authorized inspection could not establish GitHub default or organization-enforced scanning. No new scanning claim is made here. Observed product CI savings and product PR/MQ canaries remain unavailable under the activation pause.
