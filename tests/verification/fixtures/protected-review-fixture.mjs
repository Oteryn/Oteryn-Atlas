import crypto from 'node:crypto';
const digest = bytes => 'sha256:' + crypto.createHash('sha256').update(bytes).digest('hex');
const sha = character => character.repeat(40);
const hash = character => 'sha256:' + character.repeat(64);
const scenarioId = 'desktop-chromium::e2e/tests/visual-desktop.spec.mjs::full frame';
export function fixture() {
  const candidate = {repository:'Example/Atlas',prNumber:7,headSha:sha('a'),baseSha:sha('b'),treeSha:sha('c'),changedFiles:[{path:'web/example.mjs',status:'modified'}]};
  const authority = {repositoryId:99,protectedBaseSha:candidate.baseSha,workflowPath:'.github/workflows/protected-visual-capture.yml',jobName:'Capture private visual evidence',artifactName:'protected-visual-capture-42-1',runnerKind:'self-hosted',runnerGroupId:8,runnerLabels:['self-hosted','approved-private'],maxAgeMs:3600000,planDigest:hash('d'),oracleDigest:hash('e'),productDigest:hash('f'),dataCapability:'bounded_real_world',scenarioIds:[scenarioId],summaryScenarioIds:[scenarioId],requiredFrames:[{frameId:'full-frame',scenarioId}]};
  const summary = Buffer.from(JSON.stringify({status:'passed',metadata:{expectedRevision:candidate.headSha,workers:1},scenarios:[{stableTestId:scenarioId,project:'desktop-chromium',specPath:'e2e/tests/visual-desktop.spec.mjs',scenario:'full frame',status:'passed',retry:0}]}));
  const frame = Buffer.from('private full-frame bytes are only fixtures in these contract tests');
  const capture = {schemaVersion:1,kind:'protected-visual-capture',candidate,producer:{workflowPath:authority.workflowPath,sourceSha:candidate.baseSha,runId:42,jobId:43,runAttempt:1},planDigest:authority.planDigest,oracleDigest:authority.oracleDigest,productDigest:authority.productDigest,dataCapability:authority.dataCapability,scenarioIds:[scenarioId],summary:{path:'summary.json',digest:digest(summary)},frames:[{frameId:'full-frame',scenarioId,path:'private/frame.png',digest:digest(frame)}]};
  const captureBytes = Buffer.from(JSON.stringify(capture));
  const decision = {schemaVersion:1,kind:'protected-visual-review',candidate,captureDigest:digest(captureBytes),planDigest:capture.planDigest,summaryDigest:capture.summary.digest,reviewer:{id:5,login:'maintainer'},reviewedAllFrames:true,result:'PASS',frames:capture.frames.map(row=>({...row,result:'PASS'}))};
  const review = {id:44,user:{id:5,login:'maintainer'},state:'COMMENTED',commit_id:candidate.headSha,pull_request_url:'https://api.github.com/repos/Example/Atlas/pulls/7',submitted_at:'2026-09-06T10:10:00Z',body:JSON.stringify(decision)};
  return {currentCandidate:candidate,authority,captureBytes,files:[{path:'summary.json',bytes:summary},{path:'private/frame.png',bytes:frame}],captureRun:{id:42,run_attempt:1,path:authority.workflowPath,event:'workflow_dispatch',head_sha:candidate.baseSha,status:'completed',conclusion:'success',repository:{id:99,full_name:candidate.repository},created_at:'2026-09-06T10:00:00Z',updated_at:'2026-09-06T10:05:00Z'},captureJobs:{jobs:[{id:43,run_id:42,run_attempt:1,head_sha:candidate.baseSha,name:authority.jobName,status:'completed',conclusion:'success',runner_group_id:8,labels:authority.runnerLabels,started_at:'2026-09-06T10:01:00Z',completed_at:'2026-09-06T10:04:00Z'}]},captureArtifact:{id:45,name:authority.artifactName,expired:false,workflow_run:{id:42,repository_id:99,head_repository_id:99,head_sha:candidate.baseSha}},review,reviewerPermission:{permission:'admin',role_name:'admin',user:review.user},now:'2026-09-06T10:15:00Z'};
}
